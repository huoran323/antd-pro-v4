import React, { Component } from 'react';

interface DriverProps {}

interface DriverState {}

class Driver extends Component<DriverProps, DriverState> {
  render() {
    return <div>Driver</div>;
  }
}

export default Driver;
